# ── AI ──────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── MCP Server ──────────────────────────────────────────────
# 開發模式（本機）：MCP Server 跑在 docker，port 3000
MCP_SERVER_URL=http://localhost:3000

# 整合模式（docker-compose）：改用容器名稱
# MCP_SERVER_URL=http://mcp-server:3000
