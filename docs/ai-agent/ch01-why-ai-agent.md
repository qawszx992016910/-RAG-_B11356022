# Ch01 — 為什麼需要 AI Agent？

> **「程式設計的未來，不是寫更多程式碼，而是寫更好的指令。」**

---

## 🎯 本章學習目標

讀完這章，你將能夠：

- [ ] 區分「AI 工具」與「AI Agent」的本質差異
- [ ] 說出 AI Agent 的三個核心特徵
- [ ] 理解從腳本到 Agent 的演進歷程
- [ ] 判斷什麼場景適合使用 AI Agent

---

## 一個真實的故事

想像你是一位資深開發者。今天你接到一個任務：

> 「把整個專案的認證系統從 Session-based 改成 JWT。」

### 如果沒有 AI Agent...

你會這樣做：

```
1. 打開 IDE，搜尋所有和 session 相關的程式碼     →  2 小時
2. 閱讀並理解每個檔案的上下文                     →  3 小時
3. 規劃修改方案，畫架構圖                         →  2 小時
4. 逐一修改 15 個檔案                             →  4 小時
5. 寫測試、跑測試、修 Bug                         →  3 小時
6. 寫文件、提交 PR                                →  1 小時
                                        ─────────────
                                        總計 ≈ 15 小時
```

### 如果有 AI Agent...

你會這樣做：

```
1. 告訴 Agent：「分析目前的 Session 認證架構，
   規劃遷移到 JWT 的方案」                        →  Agent 花 3 分鐘
2. 審閱 Agent 的分析報告和遷移計畫                →  30 分鐘
3. 告訴 Agent：「按照計畫執行遷移」                →  Agent 花 10 分鐘
4. 審閱變更、跑測試                               →  1 小時
5. 微調和最終確認                                  →  30 分鐘
                                        ─────────────
                                        總計 ≈ 2.5 小時
```

> 🤔 **等一下，這不就是更厲害的自動補全嗎？**
>
> 不，這裡有本質的區別。讓我們深入探究。

---

## AI 工具 vs AI Agent：關鍵區別

很多人把 AI Agent 和 AI 工具搞混了。讓我們用一個比喻來釐清：

### 🔧 AI 工具 = 電動螺絲起子

```
你：「幫我鎖這顆螺絲。」
工具：*嗡嗡嗡* 鎖好了。
你：「下一顆。」
工具：*嗡嗡嗡* 鎖好了。
你：「這顆螺絲太長了。」
工具：...（沉默）
```

- 你告訴它做什麼，它就做什麼
- 每個動作都需要你的指令
- 它不知道「為什麼」要鎖螺絲
- 它不會主動告訴你螺絲太長

### 🤖 AI Agent = 有經驗的裝修師傅

```
你：「我想把這面牆裝一排書架。」
師傅：「好的，讓我先看看牆面結構...
       這面牆是承重牆，我建議用膨脹螺絲。
       這個位置有電線，我繞一下。
       對了，你放的書多重？如果超過 50 公斤，
       我建議每 60 公分加一個支撐點。」
```

- 它理解**目標**，不只是**動作**
- 它會主動**觀察環境**並做出判斷
- 遇到問題會**自主決策**或**請求確認**
- 它有**領域知識**，能提供建議

### 對照表

| 特性 | AI 工具 | AI Agent |
|------|--------|----------|
| 互動模式 | 一問一答 | 持續對話 + 自主行動 |
| 環境感知 | ❌ 不了解上下文 | ✅ 讀取檔案、理解架構 |
| 決策能力 | ❌ 只執行指令 | ✅ 分析、規劃、選擇方案 |
| 工具使用 | ❌ 只有文字輸出 | ✅ 讀寫檔案、執行命令、搜尋 |
| 記憶 | ❌ 每次對話獨立 | ✅ 跨對話記憶與學習 |
| 目標導向 | ❌ 完成單一指令 | ✅ 朝目標持續推進 |

---

## AI Agent 的三個核心特徵

一個真正的 AI Agent 必須具備三個能力：

```
            ┌──────────┐
            │  🔍 感知  │  讀取檔案、搜尋程式碼、
            │ Perceive │  理解專案結構
            └────┬─────┘
                 │
                 ▼
            ┌──────────┐
            │  🧠 推理  │  分析問題、規劃方案、
            │  Reason  │  評估風險
            └────┬─────┘
                 │
                 ▼
            ┌──────────┐
            │  ⚡ 行動  │  編輯檔案、執行命令、
            │   Act    │  提交程式碼
            └────┬─────┘
                 │
                 ▼
            ┌──────────┐
            │  🔄 觀察  │  檢查結果、驗證正確性、
            │ Observe  │  調整策略
            └──────────┘
                 │
                 └──→ 回到感知，持續循環
```

### 1. 感知（Perceive）

Agent 能「看到」環境：

```python
# Agent 可以做到的事：
- 讀取專案中的任何檔案
- 搜尋特定的程式碼片段
- 理解目錄結構和模組關係
- 分析 Git 歷史和變更記錄
- 讀取設定檔和環境變數
```

### 2. 推理（Reason）

Agent 能「思考」問題：

```python
# Agent 的推理過程：
- 「這個函式有 3 個調用者，修改它會影響到...」
- 「根據專案慣例，這裡應該使用 TypeScript 而非 JavaScript...」
- 「這段程式碼有潛在的 SQL Injection 風險，因為...」
- 「測試失敗的根因不是我的修改，而是上游的...」
```

### 3. 行動（Act）

Agent 能「動手」做事：

```python
# Agent 可以執行的動作：
- 建立、修改、刪除檔案
- 執行 Shell 命令（測試、建置、部署）
- 搜尋和替換程式碼
- 安裝套件和管理依賴
- 提交 Git 變更
```

> 💡 **關鍵洞察**：這三個能力形成一個循環。Agent 感知環境 → 推理出方案 → 執行行動 → 觀察結果 → 再次感知... 直到任務完成。

---

## 從腳本到 Agent：軟體開發自動化的演進

```
時間軸 ──────────────────────────────────────────────────→

1990s          2010s          2020          2023          2025+
  │              │              │              │              │
  ▼              ▼              ▼              ▼              ▼
┌──────┐   ┌──────────┐   ┌────────┐   ┌──────────┐   ┌──────────┐
│ 腳本  │   │ CI/CD    │   │ Copilot│   │ ChatGPT  │   │ AI Agent │
│      │   │ 自動化    │   │ 自動補全│   │ 對話式   │   │ 自主協作 │
│Shell │   │Pipeline  │   │        │   │          │   │          │
└──────┘   └──────────┘   └────────┘   └──────────┘   └──────────┘
  │              │              │              │              │
  │              │              │              │              │
 「固定流程」   「觸發執行」    「行內建議」    「問答互動」    「目標導向」
```

### 第一代：腳本自動化（1990s-2000s）

```bash
#!/bin/bash
# 固定流程，不會思考
npm install
npm run build
npm test
npm run deploy
```

**特點**：預先定義好每一步，照搬執行，遇到錯誤就停下。

### 第二代：CI/CD 管線（2010s）

```yaml
# 更聰明的觸發機制
on:
  push:
    branches: [main]
jobs:
  build:
    steps:
      - run: npm install
      - run: npm test
      - run: npm run deploy
```

**特點**：事件觸發，有條件判斷，但仍是預定義流程。

### 第三代：AI 輔助編碼（2020-2022）

```
你正在寫程式碼...
AI: 「你是不是想寫 function validateEmail(email) { ... }？」
你: *按 Tab 接受建議*
```

**特點**：智慧補全，但只在你游標所在的地方起作用，不了解全局。

### 第四代：對話式 AI（2023-2024）

```
你: 「幫我寫一個 JWT 驗證的 middleware。」
AI: 「好的，這是一個完整的 middleware...」
    （貼出一大段程式碼）
你: （複製、貼上、手動整合到專案中）
```

**特點**：能回答複雜問題，但不了解你的專案，需要你手動整合。

### 第五代：AI Agent（2025+）

```
你: 「把認證系統從 Session 改成 JWT。」
Agent: 「讓我先分析目前的認證架構...
        我找到了 15 個相關檔案。
        這是我的遷移計畫，需要你確認：
        1. 新增 JWT 工具模組 (jwt-utils.ts)
        2. 修改 auth middleware (auth.ts)
        3. 更新 6 個 API 路由
        4. 調整測試案例
        5. 更新環境變數
        要繼續嗎？」
你: 「繼續。」
Agent: *自動執行所有修改*
       「完成。所有 42 個測試通過。
        這是變更摘要：[diff]」
```

**特點**：理解專案全局、自主規劃、多步驟執行、自我驗證。

---

## 現在有哪些 AI Agent？

### 軟體開發領域

| Agent | 開發者 | 特色 |
|-------|--------|------|
| **Claude Code** | Anthropic | 終端機原生、深度推理、多工具協調 |
| **GitHub Copilot Agent** | GitHub/Microsoft | IDE 整合、程式碼審查 |
| **Cursor Agent** | Cursor | 編輯器原生、快速迭代 |
| **Devin** | Cognition | 全自主開發 Agent |
| **OpenAI Codex CLI** | OpenAI | 命令列工具、程式碼生成 |

### 通用領域

| Agent | 開發者 | 特色 |
|-------|--------|------|
| **Computer Use Agent** | Anthropic | 操作電腦桌面 |
| **AutoGPT** | Toran Bruce Richards | 自主目標達成 |
| **LangChain Agents** | LangChain | 開發框架、工具鏈 |
| **CrewAI** | CrewAI | 多 Agent 協作框架 |

> 🎯 **本手冊的焦點**：我們將以 **Claude Code** 為主要範例，
> 但所有概念和治理原則都適用於任何 AI Agent。

---

## 什麼場景適合使用 AI Agent？

### ✅ 最適合的場景

| 場景 | 為什麼適合 | 例子 |
|------|-----------|------|
| **跨檔案重構** | Agent 能同時理解多個檔案的關聯 | 重新命名函式並更新所有引用 |
| **程式碼遷移** | Agent 能分析整個系統並規劃遷移 | 從 JavaScript 遷移到 TypeScript |
| **Bug 調查** | Agent 能追蹤呼叫鏈和資料流 | 追蹤一個 null pointer 的來源 |
| **新功能開發** | Agent 能理解現有模式並保持一致 | 按照現有 API 風格新增端點 |
| **測試撰寫** | Agent 能分析程式碼路徑並生成測試 | 為整個模組補充單元測試 |

### ⚠️ 需要謹慎的場景

| 場景 | 風險 | 建議 |
|------|------|------|
| **核心演算法設計** | Agent 可能選擇次優方案 | 人類設計，Agent 實作 |
| **安全敏感操作** | 潛在的安全漏洞 | 人類審查每一行變更 |
| **資料庫 Schema 遷移** | 不可逆操作 | 人類確認後才執行 |
| **全新架構設計** | 缺乏領域特定脈絡 | 人類決策，Agent 輔助 |

### ❌ 不適合的場景

| 場景 | 為什麼不適合 |
|------|-------------|
| **需要創意突破的設計** | Agent 擅長模式匹配，不擅長跳出框架 |
| **高度政治性的技術決策** | 涉及團隊共識和組織文化 |
| **即時系統的關鍵路徑** | 延遲敏感，需要確定性保證 |

---

## 為什麼 Agent 需要「治理」？

你可能在想：「Agent 這麼厲害，直接放手讓它做不就好了？」

讓我們看看「不治理」會發生什麼：

### 😱 真實案例：沒有治理的 Agent

```
開發者：「幫我修復這個 Bug。」

Agent（沒有治理）：
  1. 讀取了整個專案 ← 浪費大量 Token
  2. 發現 Bug 在認證模組
  3. 修復 Bug 的同時「順便」重構了認證邏輯
  4. 重構過程中修改了資料庫 Schema
  5. 沒有寫遷移腳本，直接改了 production DB
  6. 刪除了「看起來沒用的」舊測試
  7. 用 force push 覆蓋了同事的 commit

結果：Bug 修好了，但製造了 5 個更大的問題。
```

### 😌 有治理的 Agent

```
開發者：「幫我修復這個 Bug。」

Agent（有治理）：
  1. 分析 Bug 的最小影響範圍 ← 精準定位
  2. 發現 Bug 在認證模組
  3. 只修改與 Bug 直接相關的程式碼 ← 最小變更原則
  4. 發現可能需要改 DB Schema
     → 🛑 停止，通知人類審查 ← 人類審查觸發
  5. 不刪除任何測試 ← 語意禁止規則
  6. 不使用 force push ← 治理憲法

結果：Bug 修好了，沒有副作用。
```

> 🎯 **核心觀點**：AI Agent 就像一個超級有能力的新員工 ——
> 能力很強，但需要清楚的規範、邊界和監督機制。
> 治理不是「限制」Agent，而是「確保」它的能力被正確運用。

---

## 本手冊的學習路徑

```
你現在在這裡
       │
       ▼
   Ch01 為什麼？   ←── 理解 Agent 的價值和定位
       │
       ▼
   Ch02 怎麼運作？  ←── 理解 Agent 的內部機制
       │
       ▼
   Ch03 動手試試    ←── 親手設定和使用第一個 Agent
       │
       ▼
   Ch04 如何溝通？  ←── 學會和 Agent 高效對話
       │
       ▼
   ...後續章節...   ←── 從使用者成長為治理者
```

---

## 章末練習

### 🧪 動手做

1. **體驗差異**：如果你手邊有 AI 工具（如 ChatGPT），試著完成以下任務：
   - 讓它解釋一段程式碼（AI 工具層級）
   - 讓它修改你專案中的某個 Bug（Agent 層級 — 它做得到嗎？）

2. **場景分析**：列出你工作中的 3 個重複性任務，評估它們是否適合交給 AI Agent。

### 🤔 思考題

1. AI Agent 和傳統自動化腳本最根本的區別是什麼？
2. 為什麼說「治理」是使用 AI Agent 的必要條件？
3. 如果一個 Agent 永遠不會犯錯，我們還需要治理嗎？為什麼？

---

## 關鍵概念回顧

| 概念 | 一句話總結 |
|------|-----------|
| AI Agent | 能感知環境、自主推理、採取行動的智慧體 |
| 感知-推理-行動 | Agent 的核心運作循環 |
| 自動化演進 | 腳本 → CI/CD → 補全 → 對話 → Agent |
| 治理 | 確保 Agent 能力被正確運用的規範框架 |

---

> **下一章預告**：[Ch02 — Agent 的大腦解剖學](ch02-agent-anatomy.md)
>
> 我們將深入 Agent 的內部，看看它的「大腦」是如何感知、思考和行動的。
> 準備好解剖一個 AI 大腦了嗎？
