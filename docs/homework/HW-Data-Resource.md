# 作業：資料收集（Data Collection）

> **課程**：資料科學導論 2026
> **對應章節**：資料收集（Data Resource Handbook）
> **繳交方式**：請將作答內容整理成一份 PDF 或 Markdown 檔案繳交

---

## 第一部分：觀念題（40 分）

### 題目 1（10 分）

請閱讀以下情境，回答問題：

> 小明想建立一個「台北市餐廳評價預測模型」。他從 Google Maps 爬取了 5,000 家餐廳的星等評分，但他發現模型預測準確率只有 48%。
>
> 經過調查，他發現：
> - 資料中有 30% 的餐廳已經歇業
> - 評分欄位有些是 `null`，有些是 `0`（但 Google Maps 最低是 1 星）
> - 資料只有「星等」，沒有「評論數量」和「餐廳類型」

請回答：

**(a)** 請列出小明的資料至少有哪 **三個** 資料品質問題？（6 分）

**(b)** 這個案例違反了資料收集五大原則中的哪幾個原則？請說明理由。（4 分）

---

### 題目 2（10 分）

請判斷以下每個問題屬於「資料收集問題」還是「模型問題」，並簡述理由：

| 編號 | 情境描述 | 資料收集問題 or 模型問題？ | 理由 |
|------|---------|--------------------------|------|
| A | 空氣品質預測模型的資料缺少「風向」變數 | | |
| B | 使用 Random Forest 但準確率不如 XGBoost | | |
| C | 股價資料的 API 每天只更新一次，但模型需要分鐘級預測 | | |
| D | 感測器故障，持續回傳固定值 | | |
| E | 模型在訓練集表現很好，但測試集表現很差（overfitting） | | |

---

### 題目 3（10 分）

請將以下資料來源分類到正確的類別中：

**類別選項**：
- (A) 學術資料集
- (B) API 即時資料
- (C) IoT 感測器
- (D) 政府開放資料
- (E) 非結構化社群資料

| 資料來源 | 類別 |
|---------|------|
| Kaggle Titanic 資料集 | |
| 民生公共物聯網 PM2.5 感測器 | |
| TDX 交通資料平台 | |
| Reddit 貼文與留言 | |
| OpenWeatherMap 天氣 API | |
| UCI Iris 資料集 | |
| data.gov.tw 人口統計 | |
| Discord 技術社群討論 | |
| Hugging Face 語言模型訓練資料 | |
| 台電即時負載數據 | |

---

### 題目 4（10 分）

請用一段文字（150-300 字）解釋：

> **為什麼「Garbage in, garbage out」是資料科學最重要的觀念之一？**

請在你的回答中至少引用課本中 **一個故事** 作為佐證。

---

## 第二部分：實作題（60 分）

### 題目 5：資料來源探索（15 分）

請從以下三個平台中，**任選兩個**，各找到一個與你有興趣的主題相關的資料集：

- [Kaggle Datasets](https://www.kaggle.com/datasets)
- [data.gov.tw](https://data.gov.tw/)
- [Google Dataset Search](https://datasetsearch.research.google.com/)

針對你找到的每個資料集，請回答：

| 項目 | 資料集 1 | 資料集 2 |
|------|---------|---------|
| 資料集名稱 | | |
| 來源平台 | | |
| 資料格式（CSV / JSON / 其他） | | |
| 資料筆數（大約） | | |
| 包含哪些主要欄位？（列出至少 5 個） | | |
| 這份資料可以用來解決什麼問題？ | | |
| 依據五大原則，這份資料的品質如何？（簡述優缺點） | | |

---

### 題目 6：API 實作（20 分）

請使用 Python 呼叫 **OpenWeatherMap API**，取得你所在城市（或台北）的即時天氣資料。

**要求**：

1. 註冊 OpenWeatherMap 取得免費 API Key
2. 撰寫 Python 程式，使用 `requests` 套件呼叫 API
3. 將回傳的 JSON 解析，並印出以下資訊：
   - 城市名稱
   - 目前溫度（攝氏）
   - 濕度
   - 天氣描述（例如：晴天、多雲）
4. 將結果存成一個 CSV 檔案

**繳交內容**：
- Python 程式碼（`.py` 或 `.ipynb`）
- 執行結果截圖
- CSV 輸出檔案

**提示**：
```python
import requests
import csv

API_KEY = "你的API金鑰"  # 請勿將 API Key 上傳到 GitHub！
city = "Taipei"
url = f"https://api.openweathermap.org/data/2.5/weather?q={city}&appid={API_KEY}&units=metric"

response = requests.get(url)
data = response.json()

# 請從 data 中取出需要的欄位...
```

> **注意**：請勿將你的 API Key 寫在繳交的程式碼中。請使用環境變數或 `.env` 檔案管理金鑰。

---

### 題目 7：資料整合規劃（25 分）

請設計一個資料科學專案的「資料收集計畫書」。

**情境**：你被要求建立一個 **YouBike 站點車輛數量預測系統**，需要預測未來 1 小時內各站點的可借車輛數。

請完成以下規劃：

**(a) 資料來源識別（10 分）**

請列出至少 **三種** 你需要的資料來源，並填寫下表：

| 資料來源 | 提供什麼資訊 | 資料格式 | 更新頻率 | 取得方式 |
|---------|------------|---------|---------|---------|
| 例：YouBike API | 各站即時車輛數 | JSON | 每分鐘 | REST API |
| | | | | |
| | | | | |
| | | | | |

**(b) 資料整合設計（10 分）**

請畫出一張資料整合流程圖（可以用文字描述或 Mermaid 語法），說明：
- 各資料來源如何匯入
- 如何進行資料整合
- 整合後的資料表包含哪些欄位

**(c) 品質檢查計畫（5 分）**

請列出至少 **三個** 你在這個專案中可能遇到的資料品質問題，以及你打算如何處理：

| 可能的資料品質問題 | 處理方式 |
|------------------|---------|
| | |
| | |
| | |

---

## 評分標準

| 部分 | 配分 | 評分重點 |
|------|------|---------|
| 第一部分：觀念題 | 40 分 | 概念正確性、邏輯清晰、引用課本內容 |
| 第二部分：實作題 | 60 分 | 完成度、程式正確性、規劃合理性 |
| **加分項目** | +5 分 | 使用台灣在地資料來源、額外探索未在課本中提到的資料平台 |

---

## 注意事項

1. **學術誠信**：請獨立完成，可以討論但不可抄襲
2. **API Key 安全**：絕對不要將 API Key 放進 Git 或繳交的檔案中
3. **格式要求**：程式碼請加上適當的註解，說明每個步驟的用途
4. **引用來源**：如果參考了課本以外的資料，請附上來源連結
