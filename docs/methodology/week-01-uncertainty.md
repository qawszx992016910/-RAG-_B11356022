# 第 1 週：不確定性的本質 — 樣本與母體

> 你看到的數字，能代表真相嗎？

---

## 🎯 學習核心

- 樣本不是事實
- 平均值是隨機變數
- 信賴區間比單點估計更重要

---

## 為什麼這件事重要？

想像你在一家電商公司擔任數據分析師。老闆問你：「上個月的平均客單價是多少？」

你查了資料庫，回答：「$1,250。」

但這個數字可靠嗎？如果你只看了某個時段的訂單、或只看了某個通路的數據，這個 $1,250 可能離「真正的平均客單價」差很遠。

**這就是不確定性的核心問題**：我們幾乎永遠只能看到樣本，而不是母體。

---

## 觀念重點

### 1️⃣ 抽樣分布（Sampling Distribution）

![抽樣分布示意圖](https://images.ctfassets.net/kj4bmrik9d6o/7gp2mVbJMMYNUtJr8VzL3u/0f7f86e92584e9fb44e22ef1b32be910/Outlier_Blog_CHARTS_SamplingDistributions_r2_FOURTH_GRAPHIC.png)

即使母體固定，每次抽樣得到的平均值都會不同。這不是錯誤，而是**抽樣的本質**。

關鍵觀念：
- 母體參數（population parameter）是固定的，但你看不到
- 樣本統計量（sample statistic）是隨機的，每次抽樣都不同
- 抽樣分布描述的是「如果你重複抽樣很多次，統計量會怎麼分布」

> 💡 **思考**：你只抽了一次樣本，但你需要理解「如果重來一次，結果可能完全不同」。

---

### 2️⃣ 中央極限定理（Central Limit Theorem）

![中央極限定理](https://i.sstatic.net/vMK1k.jpg)

這是統計學最重要的定理之一：

> 不管母體的分布長什麼樣，只要樣本數夠大（通常 n ≥ 30），樣本平均值的分布會趨近常態分布。

為什麼這很重要？
- 因為它讓我們可以用常態分布的性質來做推論
- 即使原始資料是偏態的、離散的、奇形怪狀的，平均值的行為是可預測的

```
母體分布（任意形狀）
        ↓ 重複抽樣 n 個
抽樣分布（趨近常態）
        ↓
可以計算機率、建立信賴區間
```

---

### 3️⃣ 信賴區間（Confidence Interval）

![信賴區間概念](https://dlab.berkeley.edu/sites/default/files/styles/panopoly_image_original/public/figure_2.png?itok=8ZW07yiI&timestamp=1637604129)

不要問：

> 「數字是多少？」

要問：

> 「它可能落在哪個範圍？」

**95% 信賴區間的正確解讀**：

如果我們重複抽樣 100 次，用同樣的方法各建一個信賴區間，大約有 95 個區間會包含真正的母體參數。

**常見誤解**：
- ❌ 「母體參數有 95% 的機率落在這個區間內」
- ✅ 「這個方法有 95% 的機率產生包含母體參數的區間」

> 差別很微妙，但很重要。母體參數是固定的，不是隨機的；隨機的是區間本身。

---

### 4️⃣ Bootstrap 方法

![Bootstrap 方法](https://blogs.sas.com/content/iml/files/2018/12/bootstrapSummary.png)

當你不確定抽樣分布的理論形狀時，Bootstrap 是你的好朋友：

1. 從你的樣本中「放回抽樣」（resample with replacement）
2. 每次抽出和原樣本一樣多的資料
3. 計算你要的統計量
4. 重複幾千次
5. 用這些統計量的分布來估計不確定性

**優點**：不需要假設母體分布，適用性極廣。

---

## 🧪 實作任務

### 任務 A：體驗抽樣變異

使用 Python 完成以下步驟：

```python
import numpy as np
import matplotlib.pyplot as plt

# 1. 建立一個「母體」（假裝這是全部的資料）
np.random.seed(42)
population = np.random.exponential(scale=1000, size=100000)

# 2. 從母體中隨機抽樣 30 筆
sample = np.random.choice(population, size=30, replace=False)

# 3. 重複抽樣 1000 次，記錄每次的平均值
sample_means = []
for _ in range(1000):
    s = np.random.choice(population, size=30, replace=False)
    sample_means.append(s.mean())

# 4. 畫出抽樣分布
plt.hist(sample_means, bins=50, edgecolor='black', alpha=0.7)
plt.axvline(np.mean(population), color='red', linestyle='--', label='母體平均')
plt.xlabel('樣本平均值')
plt.ylabel('次數')
plt.title('抽樣分布（n=30, 重複 1000 次）')
plt.legend()
plt.show()

# 5. 計算 95% 信賴區間
ci_lower = np.percentile(sample_means, 2.5)
ci_upper = np.percentile(sample_means, 97.5)
print(f"95% 信賴區間：[{ci_lower:.1f}, {ci_upper:.1f}]")
print(f"母體真正的平均值：{np.mean(population):.1f}")
```

### 任務 B：觀察樣本數的影響

修改上面的程式，分別用 n = 10, 30, 100, 500 做同樣的實驗。

將四張直方圖放在同一張圖上（提示：`plt.subplot`），觀察：
- 分布的寬窄如何變化？
- 信賴區間的寬度如何變化？

### 任務 C：Bootstrap 實作

```python
# 使用你的「一次」樣本，用 Bootstrap 估計信賴區間
sample = np.random.choice(population, size=30, replace=False)

boot_means = []
for _ in range(5000):
    boot_sample = np.random.choice(sample, size=len(sample), replace=True)
    boot_means.append(boot_sample.mean())

ci_lower = np.percentile(boot_means, 2.5)
ci_upper = np.percentile(boot_means, 97.5)
print(f"Bootstrap 95% CI: [{ci_lower:.1f}, {ci_upper:.1f}]")
```

**比較**：Bootstrap 得到的信賴區間與任務 A 的結果差多少？為什麼？

---

## 🧠 反思問題

請用 3-5 句話回答每個問題，並寫入你的學習筆記：

1. **樣本數增加，信賴區間會如何變化？** 試著用直覺和數學兩個角度解釋。

2. **若信賴區間包含 0，代表什麼？** 想像你在比較兩組用戶的轉換率差異，信賴區間是 [-0.02, 0.05]。你會怎麼向老闆報告？

3. **為什麼 p-value 可能誤導？** 預告下週的主題 — 先用本週學到的「不確定性」觀念想想看。

4. **在你的日常生活或實習經驗中，有哪些「只看了樣本就下結論」的例子？**

---

## 延伸閱讀

- [Seeing Theory - Frequentist Inference](https://seeing-theory.brown.edu/frequentist-inference/) — 互動式視覺化
- [Statistics 110: Probability (Harvard)](https://projects.iq.harvard.edu/stat110) — 機率基礎
- Efron & Tibshirani, *An Introduction to the Bootstrap* — Bootstrap 方法經典教材

---

## 本週 Checklist

- [ ] 完成任務 A：畫出抽樣分布圖
- [ ] 完成任務 B：比較不同樣本數的信賴區間
- [ ] 完成任務 C：Bootstrap 信賴區間
- [ ] 回答全部反思問題
- [ ] 將程式碼與筆記推送至 GitHub

---

[→ 下一週：假設檢定與其陷阱](week-02-hypothesis-testing.md)
