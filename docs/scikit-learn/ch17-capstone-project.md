# 第十七章：專案製作與期末發表 — 把一切串起來

> 「學武功要實戰，學 ML 要做專案。」

```
第 1-16 章的你：                  做完專案的你：

  "我懂 Random Forest"              "我知道什麼時候該用
   "我會調超參數"                     Random Forest，
   "我知道 cross validation"          什麼時候不該用，
                                      以及怎麼說服老闆
                                      為什麼要用它。"

   知識 ──────────────────────────► 能力
```

---

## 🎯 本章目標

完成本章後，你將能夠：

1. 定義一個明確的機器學習問題
2. 完成端到端的 ML 專案流程
3. 比較至少 3 種模型並合理解釋選擇
4. 使用 cross validation 做可靠的模型評估
5. 進行 bias-variance 分析
6. 撰寫專業的評估報告與商業建議
7. 準備期末發表所需的全部交付項目

---

## 17.1 專案要求總覽

### 七大核心要素

```
完整 ML 專案的七大要素：

  ┌──────────────────────────────────────────┐
  │                                            │
  │  1. 問題定義          ← 最重要，卻最常被忽略 │
  │     ↓                                      │
  │  2. 資料處理                                │
  │     ↓                                      │
  │  3. 模型比較（至少 3 種）                    │
  │     ↓                                      │
  │  4. Cross Validation                       │
  │     ↓                                      │
  │  5. Bias-Variance 分析                     │
  │     ↓                                      │
  │  6. 評估分析                                │
  │     ↓                                      │
  │  7. 商業建議                                │
  │                                            │
  └──────────────────────────────────────────┘
```

---

## 17.2 問題定義：你在解決什麼？

### 好的問題定義 vs 壞的問題定義

```
❌ 壞的問題定義：
"我要用機器學習預測東西"
"我要做一個很準的模型"
"我找了一個 Kaggle 資料集"

✅ 好的問題定義：
"電商公司想預測哪些客戶在未來 30 天內
 會流失（不再購買），以便提前介入挽留。
 目標：識別出至少 80% 即將流失的客戶
 （recall ≥ 0.8），同時控制誤報率
 （precision ≥ 0.5）以免浪費行銷資源。"
```

### 問題定義模板

```
+----------------------------------------------------------+
|  問題定義模板                                              |
+----------------------------------------------------------+
|                                                            |
|  1. 業務背景：                                             |
|     公司/組織面臨什麼問題？                                |
|     為什麼需要 ML 來解決？                                 |
|     _____________________________________________          |
|                                                            |
|  2. 目標定義：                                             |
|     預測什麼？（y 是什麼？）                               |
|     分類還是回歸？                                         |
|     _____________________________________________          |
|                                                            |
|  3. 成功標準：                                             |
|     用什麼指標衡量成功？                                   |
|     目標值是多少？為什麼？                                 |
|     _____________________________________________          |
|                                                            |
|  4. 限制條件：                                             |
|     模型需要可解釋嗎？                                     |
|     有時間/計算資源限制嗎？                                |
|     有公平性/倫理考量嗎？                                  |
|     _____________________________________________          |
|                                                            |
|  5. 影響分析：                                             |
|     False Positive 的代價是什麼？                          |
|     False Negative 的代價是什麼？                          |
|     哪個更嚴重？                                           |
|     _____________________________________________          |
|                                                            |
+----------------------------------------------------------+
```

### 💡 重點觀念

> **問題定義決定了一切。** 選錯指標、搞錯目標、忽略限制條件，
> 再好的模型也沒用。花 20% 的時間在問題定義上，可以節省
> 80% 走冤枉路的時間。

---

## 17.3 資料處理：垃圾進，垃圾出

### 資料處理流程圖

```
原始資料
    │
    ├── 1. 資料探索 (EDA)
    │   ├── 資料型態檢查
    │   ├── 缺失值統計
    │   ├── 分布圖 & 描述統計
    │   └── 相關性分析
    │
    ├── 2. 資料清理
    │   ├── 處理缺失值
    │   ├── 處理異常值
    │   ├── 修正資料型態
    │   └── 處理重複資料
    │
    ├── 3. 特徵工程
    │   ├── 編碼類別變數
    │   ├── 特徵縮放
    │   ├── 特徵選擇
    │   └── 特徵創造
    │
    └── 4. 資料分割
        ├── Train / Validation / Test
        └── 注意：時間序列要按時間分割！

    ⚠️  所有轉換都要 fit on train, transform on test
```

```python
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler, LabelEncoder
from sklearn.impute import SimpleImputer
from sklearn.pipeline import Pipeline
from sklearn.compose import ColumnTransformer

# === EDA 模板 ===
def quick_eda(df, target_col=None):
    """快速資料探索"""
    print("="*50)
    print(f"資料形狀: {df.shape}")
    print(f"欄位數: {df.shape[1]}")
    print(f"樣本數: {df.shape[0]}")
    print("="*50)

    print("\n--- 資料型態 ---")
    print(df.dtypes.value_counts())

    print("\n--- 缺失值 ---")
    missing = df.isnull().sum()
    missing_pct = (missing / len(df)) * 100
    missing_df = pd.DataFrame({
        'Count': missing,
        'Percentage': missing_pct
    })
    print(missing_df[missing_df['Count'] > 0].sort_values(
        'Percentage', ascending=False
    ))

    print("\n--- 描述統計 ---")
    print(df.describe())

    if target_col:
        print(f"\n--- 目標變數分布 ({target_col}) ---")
        print(df[target_col].value_counts(normalize=True))


# === 建立處理 Pipeline ===
def build_preprocessing_pipeline(numeric_features, categorical_features):
    """建立資料前處理管線"""

    numeric_pipeline = Pipeline([
        ('imputer', SimpleImputer(strategy='median')),
        ('scaler', StandardScaler())
    ])

    categorical_pipeline = Pipeline([
        ('imputer', SimpleImputer(strategy='most_frequent')),
        ('encoder', LabelEncoder())  # 實際上用 OneHotEncoder 更好
    ])

    # 注意：實際用 ColumnTransformer 組合
    preprocessor = ColumnTransformer([
        ('num', numeric_pipeline, numeric_features),
        ('cat', categorical_pipeline, categorical_features)
    ])

    return preprocessor
```

### ⚠️ 常見陷阱

```
陷阱 1：在整個資料集上做 fit_transform
─────────────────────────────────────────────
❌ scaler.fit_transform(X)  # 包含 test 的資訊 → 資料洩漏！
✅ scaler.fit(X_train)
   X_train = scaler.transform(X_train)
   X_test = scaler.transform(X_test)

陷阱 2：時間序列用隨機分割
─────────────────────────────────────────────
❌ train_test_split(X, y, random_state=42)  # 未來資料混入訓練集
✅ 按時間順序分割，用過去預測未來

陷阱 3：EDA 做得不夠
─────────────────────────────────────────────
花在 EDA 上的每一分鐘，都能在建模時省下十分鐘。
不要急著跑模型！先理解你的資料。

陷阱 4：忘記處理類別變數的新類別
─────────────────────────────────────────────
如果 test 中出現了 train 沒有的類別值怎麼辦？
需要有 handle_unknown='ignore' 的策略。
```

---

## 17.4 模型比較：至少三種，但要有理由

### 模型選擇策略

```
如何選擇要比較的模型？

  資料特性                 建議模型組合
  ──────────              ──────────────
  小資料集 (<1K)     →    LR, SVM, KNN
  中資料集 (1K-100K) →    RF, GBM, SVM
  大資料集 (>100K)   →    GBM, LR, Neural Net
  需要可解釋性       →    LR, DT, Rule-based
  高維度資料         →    LR+L1, RF, PCA+模型
  不平衡資料         →    RF(balanced), GBM, LR(balanced)

最佳實踐：選擇不同「家族」的模型
  ┌─────────────────────────────────────────────┐
  │ 線性家族：Logistic Regression, SVM (linear) │
  │ 樹家族：  Random Forest, GBM, XGBoost       │
  │ 距離家族：KNN, SVM (RBF)                    │
  │ 集成家族：Voting, Stacking                  │
  └─────────────────────────────────────────────┘
```

```python
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import (
    RandomForestClassifier,
    GradientBoostingClassifier
)
from sklearn.svm import SVC
from sklearn.model_selection import cross_val_score
from sklearn.metrics import make_scorer, f1_score
import time

# === 模型比較框架 ===
def compare_models(X_train, y_train, scoring='f1'):
    """
    比較多個模型的 cross-validation 表現
    """
    models = {
        'Logistic Regression': LogisticRegression(
            max_iter=1000, random_state=42
        ),
        'Random Forest': RandomForestClassifier(
            n_estimators=100, random_state=42
        ),
        'Gradient Boosting': GradientBoostingClassifier(
            n_estimators=100, random_state=42
        ),
    }

    results = {}

    print(f"{'Model':<25} {'Mean':>8} {'Std':>8} {'Time':>8}")
    print("-" * 55)

    for name, model in models.items():
        start = time.time()

        scores = cross_val_score(
            model, X_train, y_train,
            cv=5, scoring=scoring, n_jobs=-1
        )

        elapsed = time.time() - start

        results[name] = {
            'mean': scores.mean(),
            'std': scores.std(),
            'scores': scores,
            'time': elapsed
        }

        print(f"{name:<25} {scores.mean():>8.4f} {scores.std():>8.4f} {elapsed:>7.1f}s")

    # 找出最佳模型
    best = max(results, key=lambda x: results[x]['mean'])
    print(f"\n最佳模型: {best} (Mean {scoring}: {results[best]['mean']:.4f})")

    return results


# === 使用範例 ===
# results = compare_models(X_train, y_train, scoring='f1')
```

### ❓ 沒有笨問題

**Q：只比較 3 個模型夠嗎？**
> A：3 個是最低要求。重點不是數量，而是「多樣性」。
> 選擇不同類型的模型（線性、樹、距離），這樣你才能了解
> 什麼類型的方法最適合你的問題。

**Q：如果兩個模型表現差不多怎麼辦？**
> A：這時候考慮其他因素：可解釋性、訓練/推論速度、
> 對新資料的穩健性、維護成本。更簡單的模型通常更好。

**Q：一定要用 scikit-learn 的模型嗎？**
> A：不一定。你可以用 XGBoost、LightGBM 等。但確保
> 你理解每個模型的原理，不要只是「試試看」。

---

## 17.5 Cross Validation：可靠的模型評估

### Cross Validation 策略選擇

```
你的資料是什麼類型？

  ├── 一般資料（i.i.d.）
  │   ├── 資料量足夠 → K-Fold CV (K=5 或 10)
  │   ├── 資料量少   → Leave-One-Out CV
  │   └── 不平衡     → Stratified K-Fold CV
  │
  ├── 時間序列
  │   └── TimeSeriesSplit（只用過去預測未來）
  │
  └── 群組資料（同一人多筆）
      └── GroupKFold（同一群組不跨 fold）
```

```python
from sklearn.model_selection import (
    cross_validate,
    StratifiedKFold,
    RepeatedStratifiedKFold
)

# === 完整的 Cross Validation 報告 ===
def detailed_cv_report(model, X, y, cv=5):
    """
    生成詳細的 cross-validation 報告
    """
    scoring = {
        'accuracy': 'accuracy',
        'precision': 'precision',
        'recall': 'recall',
        'f1': 'f1',
        'roc_auc': 'roc_auc'
    }

    # 使用 Stratified K-Fold 確保每個 fold 的類別比例一致
    skf = StratifiedKFold(n_splits=cv, shuffle=True, random_state=42)

    results = cross_validate(
        model, X, y,
        cv=skf,
        scoring=scoring,
        return_train_score=True,
        n_jobs=-1
    )

    print("="*60)
    print("Cross-Validation 報告")
    print("="*60)

    print(f"\n{'Metric':<15} {'Train Mean':>12} {'Test Mean':>12} {'Test Std':>10} {'Gap':>8}")
    print("-"*60)

    for metric in scoring:
        train_key = f'train_{metric}'
        test_key = f'test_{metric}'

        train_mean = results[train_key].mean()
        test_mean = results[test_key].mean()
        test_std = results[test_key].std()
        gap = train_mean - test_mean

        # 標記可能的問題
        flag = ""
        if gap > 0.1:
            flag = " ⚠️ 過擬合"
        elif test_std > 0.1:
            flag = " ⚠️ 不穩定"

        print(f"{metric:<15} {train_mean:>12.4f} {test_mean:>12.4f} "
              f"{test_std:>10.4f} {gap:>8.4f}{flag}")

    print(f"\n訓練時間: {results['fit_time'].mean():.2f}s (avg)")

    return results

# === 使用範例 ===
# results = detailed_cv_report(model, X_train, y_train, cv=5)
```

---

## 17.6 Bias-Variance 分析

### 用學習曲線診斷問題

```
高偏差 (Underfitting)：            高變異 (Overfitting)：

Score                              Score
  ^                                  ^
  |  --------- training             |  --------- training
  |                                  |
  |                                  |
  |  --------- validation           |
  |                                  |  --------- validation
  |                                  |
  +-------------------->            +-------------------->
    Training Set Size                 Training Set Size

兩條線都低且接近                    兩條線差距大
→ 模型太簡單                        → 模型太複雜
→ 加特徵、用更複雜的模型            → 加正則化、減特徵、加資料
```

```python
from sklearn.model_selection import learning_curve
import matplotlib.pyplot as plt
import numpy as np

def plot_learning_curve(estimator, X, y, title="Learning Curve",
                        cv=5, scoring='f1'):
    """
    繪製學習曲線，診斷 bias-variance 問題
    """
    train_sizes, train_scores, val_scores = learning_curve(
        estimator, X, y,
        cv=cv,
        scoring=scoring,
        train_sizes=np.linspace(0.1, 1.0, 10),
        n_jobs=-1,
        random_state=42
    )

    train_mean = train_scores.mean(axis=1)
    train_std = train_scores.std(axis=1)
    val_mean = val_scores.mean(axis=1)
    val_std = val_scores.std(axis=1)

    plt.figure(figsize=(10, 6))
    plt.title(title)

    # 訓練分數
    plt.plot(train_sizes, train_mean, 'o-', color='blue',
             label=f'Training {scoring}')
    plt.fill_between(train_sizes,
                     train_mean - train_std,
                     train_mean + train_std,
                     alpha=0.1, color='blue')

    # 驗證分數
    plt.plot(train_sizes, val_mean, 'o-', color='red',
             label=f'Validation {scoring}')
    plt.fill_between(train_sizes,
                     val_mean - val_std,
                     val_mean + val_std,
                     alpha=0.1, color='red')

    plt.xlabel('Training Set Size')
    plt.ylabel(scoring)
    plt.legend(loc='best')
    plt.grid(True)

    # 診斷
    gap = train_mean[-1] - val_mean[-1]
    print(f"\n=== Bias-Variance 診斷 ===")
    print(f"最終 Training {scoring}: {train_mean[-1]:.4f}")
    print(f"最終 Validation {scoring}: {val_mean[-1]:.4f}")
    print(f"Gap: {gap:.4f}")

    if gap > 0.15:
        print("診斷: 高變異 (Overfitting)")
        print("建議: 增加正則化、減少特徵、收集更多資料")
    elif val_mean[-1] < 0.6:
        print("診斷: 高偏差 (Underfitting)")
        print("建議: 使用更複雜的模型、增加特徵、減少正則化")
    else:
        print("診斷: 模型表現良好")

    plt.tight_layout()
    plt.show()

    return train_sizes, train_mean, val_mean
```

### 🧠 動動腦

> 你的模型 training accuracy 是 99%，但 validation accuracy 只有 72%。
>
> 1. 這是 high bias 還是 high variance？
> 2. 以下哪些策略可能有效？
>    a) 收集更多資料
>    b) 增加更多特徵
>    c) 增加正則化
>    d) 使用更簡單的模型
>    e) 使用更複雜的模型
>    f) 減少特徵數量

---

## 17.7 評估分析與商業建議

### 評估報告模板

```python
def generate_evaluation_report(model, X_test, y_test, model_name="Model"):
    """
    生成完整的模型評估報告
    """
    from sklearn.metrics import (
        classification_report, confusion_matrix,
        roc_auc_score, roc_curve,
        precision_recall_curve, average_precision_score
    )

    y_pred = model.predict(X_test)
    y_prob = model.predict_proba(X_test)[:, 1]

    print("="*60)
    print(f"  模型評估報告：{model_name}")
    print("="*60)

    # 1. 基本指標
    print("\n--- 1. 分類報告 ---")
    print(classification_report(y_test, y_pred))

    # 2. 混淆矩陣
    print("--- 2. 混淆矩陣 ---")
    cm = confusion_matrix(y_test, y_pred)
    print(f"               Predicted")
    print(f"               Neg    Pos")
    print(f"  Actual Neg [{cm[0,0]:>5}  {cm[0,1]:>5}]")
    print(f"  Actual Pos [{cm[1,0]:>5}  {cm[1,1]:>5}]")

    # 3. AUC-ROC
    auc = roc_auc_score(y_test, y_prob)
    print(f"\n--- 3. AUC-ROC: {auc:.4f} ---")

    # 4. 商業影響分析
    tn, fp, fn, tp = cm.ravel()
    total = len(y_test)

    print("\n--- 4. 商業影響分析 ---")
    print(f"  正確處理的案例: {(tp+tn)/total*100:.1f}%")
    print(f"  誤報 (False Positive): {fp} 件 ({fp/total*100:.1f}%)")
    print(f"    → 可能的成本: 不必要的介入/調查")
    print(f"  漏報 (False Negative): {fn} 件 ({fn/total*100:.1f}%)")
    print(f"    → 可能的成本: 錯過真正需要處理的案例")

    return {
        'auc': auc,
        'confusion_matrix': cm,
        'y_pred': y_pred,
        'y_prob': y_prob
    }
```

### 商業建議的寫法

```
+----------------------------------------------------------+
|  商業建議撰寫框架                                          |
+----------------------------------------------------------+
|                                                            |
|  1. 執行摘要（30 秒版）                                   |
|     "我們的模型可以在 XX 場景下，將 YY 提升 ZZ%，         |
|      預估每年可節省/創造 $$$。"                            |
|                                                            |
|  2. 模型表現摘要（非技術語言）                             |
|     "在 1000 個測試案例中，模型：                          |
|      - 正確識別了 85% 的目標客戶                           |
|      - 每 10 個預測中有 7 個是正確的                       |
|      - 比現有方法提升了 20%"                               |
|                                                            |
|  3. 風險與限制（誠實面對）                                 |
|     "模型在以下情況可能表現不佳：                          |
|      - 新客戶（缺少歷史資料）                              |
|      - 特殊促銷期間（行為模式改變）                        |
|      - 某些客群（資料不足）"                               |
|                                                            |
|  4. 部署建議                                               |
|     "建議分階段部署：                                      |
|      Phase 1: 小規模測試 (A/B test)                       |
|      Phase 2: 擴大範圍，建立監控                           |
|      Phase 3: 全面部署，持續優化"                          |
|                                                            |
|  5. 下一步行動                                             |
|     具體的、可執行的下一步建議                             |
|                                                            |
+----------------------------------------------------------+
```

---

## 17.8 交付項目清單

### 必要交付項目

```
你的 GitHub Repo 結構應該像這樣：

my-ml-project/
├── README.md                 ← 專案說明（最重要的檔案！）
├── requirements.txt          ← 套件依賴
├── data/
│   ├── raw/                  ← 原始資料（或資料下載說明）
│   └── processed/            ← 處理後的資料
├── notebooks/
│   ├── 01_EDA.ipynb          ← 資料探索
│   ├── 02_preprocessing.ipynb← 資料前處理
│   ├── 03_modeling.ipynb     ← 模型訓練與比較
│   └── 04_evaluation.ipynb   ← 評估與分析
├── src/                      ← （進階）可重用的程式碼
│   ├── data_utils.py
│   ├── model_utils.py
│   └── eval_utils.py
├── reports/
│   ├── figures/              ← 圖表
│   └── evaluation_report.md  ← 評估報告
└── presentation/
    └── slides.pdf            ← 發表簡報
```

### README 模板

```markdown
# 專案名稱

## 問題描述
（2-3 句話說明你在解決什麼問題）

## 資料來源
- 來源：
- 樣本數：
- 特徵數：
- 目標變數：

## 方法
### 資料前處理
- ...

### 模型比較
| 模型 | Accuracy | F1 | AUC-ROC | 訓練時間 |
|------|----------|-----|---------|---------|
| ...  | ...      | ... | ...     | ...     |

### 最佳模型
- 選擇了 XXX 模型，因為...

## 結果
（關鍵發現，用非技術語言）

## 模型限制
（誠實說明模型的限制和適用範圍）

## 如何復現
```bash
pip install -r requirements.txt
jupyter notebook notebooks/01_EDA.ipynb
```

## 未來改進方向
-（如果有更多時間/資料，你會做什麼？）
```

### 實驗流程圖

```
你需要提供一張清楚的實驗流程圖：

┌──────────┐     ┌──────────────┐     ┌──────────────┐
│  原始資料  │────→│   EDA        │────→│  資料前處理   │
│  N 筆      │     │  分布、相關性 │     │  缺失值、縮放 │
└──────────┘     └──────────────┘     └──────────────┘
                                             │
                      ┌──────────────────────┤
                      │                      │
                      v                      v
              ┌──────────────┐     ┌──────────────┐
              │  Train Set    │     │  Test Set     │
              │  (80%)        │     │  (20%)        │
              └──────────────┘     └──────────────┘
                      │                      │
          ┌───────────┼───────────┐          │
          │           │           │          │
          v           v           v          │
    ┌─────────┐ ┌─────────┐ ┌─────────┐    │
    │ Model A │ │ Model B │ │ Model C │    │
    │ (LR)    │ │ (RF)    │ │ (GBM)   │    │
    └─────────┘ └─────────┘ └─────────┘    │
          │           │           │          │
          └───────────┼───────────┘          │
                      │ 5-Fold CV            │
                      v                      │
              ┌──────────────┐               │
              │  最佳模型選擇  │               │
              │  + 調參        │               │
              └──────────────┘               │
                      │                      │
                      v                      v
              ┌──────────────────────────────┐
              │      最終評估 (Test Set)       │
              │  Accuracy, F1, AUC-ROC,      │
              │  Confusion Matrix, 學習曲線   │
              └──────────────────────────────┘
                             │
                             v
              ┌──────────────────────────────┐
              │      商業建議與限制討論        │
              └──────────────────────────────┘
```

---

## 17.9 評分標準

### 💡 重點觀念

> **流程正確比 accuracy 高更重要。**
> 一個 accuracy 只有 75% 但流程完整、分析深入的專案，
> 遠勝過 accuracy 95% 但沒有 cross validation、
> 沒有 bias-variance 分析的專案。

### 評分規準 (Rubric)

```
+--------------------------------------------------------------+
|  評分項目                 | 配分 | 優秀 | 良好 | 待改進       |
+--------------------------------------------------------------+
|                           |      |      |      |              |
|  1. 問題定義              | 15%  |      |      |              |
|     - 清晰度              |  5%  | 目標明確 | 稍模糊 | 不清楚  |
|     - 指標選擇合理性      |  5%  | 有解釋 | 選了但沒解釋 | 沒選|
|     - 業務脈絡            |  5%  | 完整 | 部分 | 缺少        |
|                           |      |      |      |              |
|  2. 資料處理              | 20%  |      |      |              |
|     - EDA 完整性          |  8%  | 深入 | 基本 | 草率        |
|     - 前處理正確性        |  7%  | 無洩漏 | 小問題 | 有洩漏  |
|     - 特徵工程            |  5%  | 有創意 | 基本 | 沒做      |
|                           |      |      |      |              |
|  3. 模型比較              | 20%  |      |      |              |
|     - 至少 3 種模型       |  5%  | 3+ 種 | 2 種 | 1 種      |
|     - 選擇有理由          |  5%  | 有解釋 | 少解釋 | 沒解釋  |
|     - Cross Validation    |  5%  | 正確使用 | 有用但有問題 | 沒用|
|     - 超參數調整          |  5%  | GridSearch | 手動 | 預設  |
|                           |      |      |      |              |
|  4. 分析深度              | 20%  |      |      |              |
|     - Bias-Variance 分析  |  7%  | 有曲線+解讀 | 有曲線 | 沒做|
|     - 錯誤分析            |  7%  | 深入 | 基本 | 沒做      |
|     - 模型限制討論        |  6%  | 誠實深入 | 提到 | 沒提  |
|                           |      |      |      |              |
|  5. 商業建議              | 10%  |      |      |              |
|     - 可行性              |  5%  | 具體可執行 | 太籠統 | 沒有|
|     - 風險評估            |  5%  | 有考量 | 少提 | 沒有     |
|                           |      |      |      |              |
|  6. 報告與簡報品質        | 15%  |      |      |              |
|     - README 完整性       |  5%  | 完整 | 基本 | 缺少      |
|     - 程式碼品質          |  5%  | 清楚註解 | 可讀 | 混亂  |
|     - 簡報表達            |  5%  | 清楚專業 | 基本 | 不清楚|
|                           |      |      |      |              |
+--------------------------------------------------------------+
|  總分                     | 100% |                            |
+--------------------------------------------------------------+

加分項目 (Bonus):
  + 使用 Pipeline 建立完整流程
  + 做了公平性/倫理分析（第 16 章）
  + 部署了 demo（Streamlit / Gradio）
  + 做了特徵重要性的深入分析
  + 使用了 SHAP 或 LIME 做可解釋性
```

---

## 17.10 專案模板與 Checklist

### 期末專案檢查清單

```
+----------------------------------------------------------+
|  期末專案完成度自我檢查                                    |
+----------------------------------------------------------+
|                                                            |
|  □ 問題定義                                               |
|    □ 問題描述清楚                                         |
|    □ 目標變數定義明確                                     |
|    □ 成功標準已設定                                       |
|    □ 影響分析已完成                                       |
|                                                            |
|  □ 資料處理                                               |
|    □ EDA 報告完成                                         |
|    □ 缺失值已處理                                         |
|    □ 資料分割正確（無資料洩漏）                           |
|    □ 前處理 Pipeline 建立                                 |
|                                                            |
|  □ 模型比較                                               |
|    □ 至少 3 種不同類型的模型                              |
|    □ 每個模型有 5-Fold CV 結果                            |
|    □ 模型選擇有明確理由                                   |
|    □ 超參數調整已完成                                     |
|                                                            |
|  □ 分析                                                   |
|    □ 學習曲線已繪製                                       |
|    □ Bias-Variance 診斷已完成                             |
|    □ 混淆矩陣已分析                                       |
|    □ 錯誤分析已完成                                       |
|    □ 特徵重要性已分析                                     |
|                                                            |
|  □ 報告                                                   |
|    □ 模型限制已討論                                       |
|    □ 商業建議已撰寫                                       |
|    □ 評估報告完整                                         |
|                                                            |
|  □ 交付物                                                 |
|    □ GitHub Repo 整理乾淨                                 |
|    □ README 完整                                          |
|    □ requirements.txt 正確                                |
|    □ 程式碼有註解                                         |
|    □ 簡報 slides 準備好                                   |
|                                                            |
+----------------------------------------------------------+
```

---

## 17.11 完整範例專案演練：客戶流失預測

### 步驟一：問題定義

```
業務背景：某電信公司每月流失約 5% 的客戶，
          每流失一個客戶的成本約為 $500。

目標：建立一個預測模型，識別即將流失的客戶，
      讓客服團隊提前介入挽留。

成功標準：
  - Recall ≥ 0.80（不能錯過太多即將流失的客戶）
  - Precision ≥ 0.50（不要浪費太多行銷資源在不會流失的客戶上）

影響分析：
  - False Negative（漏報）：錯過即將流失的客戶 → 損失 $500/人
  - False Positive（誤報）：對不會流失的客戶做挽留 → 花費 $50/人
  - FN 的代價是 FP 的 10 倍 → Recall 比 Precision 重要
```

### 步驟二：資料處理（程式碼摘要）

```python
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler, OneHotEncoder
from sklearn.compose import ColumnTransformer
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer

# 載入資料（以 Telco Customer Churn 為例）
# df = pd.read_csv('data/raw/telco_churn.csv')

# EDA 摘要
# quick_eda(df, target_col='Churn')

# 資料前處理
numeric_features = ['tenure', 'MonthlyCharges', 'TotalCharges']
categorical_features = ['Contract', 'PaymentMethod', 'InternetService']

preprocessor = ColumnTransformer([
    ('num', Pipeline([
        ('imputer', SimpleImputer(strategy='median')),
        ('scaler', StandardScaler())
    ]), numeric_features),
    ('cat', Pipeline([
        ('imputer', SimpleImputer(strategy='most_frequent')),
        ('encoder', OneHotEncoder(handle_unknown='ignore'))
    ]), categorical_features)
])

# 分割資料
# X_train, X_test, y_train, y_test = train_test_split(
#     X, y, test_size=0.2, stratify=y, random_state=42
# )
```

### 步驟三：模型比較

```python
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from sklearn.model_selection import cross_val_score, GridSearchCV

# 三個模型 + Pipeline
models = {
    'Logistic Regression': Pipeline([
        ('preprocessor', preprocessor),
        ('classifier', LogisticRegression(
            class_weight='balanced', max_iter=1000, random_state=42
        ))
    ]),
    'Random Forest': Pipeline([
        ('preprocessor', preprocessor),
        ('classifier', RandomForestClassifier(
            class_weight='balanced', n_estimators=100, random_state=42
        ))
    ]),
    'Gradient Boosting': Pipeline([
        ('preprocessor', preprocessor),
        ('classifier', GradientBoostingClassifier(
            n_estimators=100, random_state=42
        ))
    ]),
}

# Cross Validation 比較
# 使用 recall 作為主要指標（因為 FN 代價高）
for name, model in models.items():
    scores = cross_val_score(
        model, X_train, y_train,
        cv=5, scoring='recall', n_jobs=-1
    )
    print(f"{name}: Recall = {scores.mean():.4f} (+/- {scores.std():.4f})")
```

### 步驟四：超參數調整

```python
# 假設 GBM 表現最好，進行調參
param_grid = {
    'classifier__n_estimators': [100, 200, 300],
    'classifier__max_depth': [3, 5, 7],
    'classifier__learning_rate': [0.01, 0.05, 0.1],
    'classifier__min_samples_leaf': [5, 10, 20]
}

grid_search = GridSearchCV(
    models['Gradient Boosting'],
    param_grid,
    cv=5,
    scoring='recall',
    n_jobs=-1,
    verbose=1
)

# grid_search.fit(X_train, y_train)
# print(f"Best params: {grid_search.best_params_}")
# print(f"Best recall: {grid_search.best_score_:.4f}")
```

### 步驟五：最終評估

```python
# 用最佳模型在 Test Set 上做最終評估
# best_model = grid_search.best_estimator_
# report = generate_evaluation_report(best_model, X_test, y_test, "GBM (Tuned)")

# 學習曲線
# plot_learning_curve(best_model, X_train, y_train, scoring='recall')
```

### 步驟六：商業建議

```
+----------------------------------------------------------+
|  商業建議：客戶流失預測模型                               |
+----------------------------------------------------------+
|                                                            |
|  執行摘要：                                               |
|  我們的模型可以識別出 82% 即將流失的客戶，               |
|  如果對這些客戶進行挽留，預估每月可減少                   |
|  150 位客戶流失，節省約 $75,000/月。                     |
|                                                            |
|  部署建議：                                               |
|  Phase 1 (第 1 月)：                                      |
|    - 在 10% 的客群上進行 A/B 測試                        |
|    - 建立自動化的挽留觸發流程                             |
|                                                            |
|  Phase 2 (第 2-3 月)：                                    |
|    - 擴大到 50%，根據測試結果調整                        |
|    - 建立模型表現監控 dashboard                           |
|                                                            |
|  Phase 3 (第 4 月起)：                                    |
|    - 全面部署                                             |
|    - 每月重新評估模型表現                                 |
|    - 每季重新訓練模型                                     |
|                                                            |
|  風險與限制：                                              |
|  - 模型基於歷史資料，市場環境變化可能影響準確性           |
|  - 新客戶（< 3 個月）的預測可靠性較低                    |
|  - 建議搭配人工判斷，不要完全自動化                       |
|                                                            |
+----------------------------------------------------------+
```

---

## 17.12 期末發表技巧

### 簡報結構建議（10 分鐘發表）

```
時間分配：
+----------------------------------------------------------+
|  1. 問題與動機 (2 min)                                    |
|     - 為什麼這個問題重要？                                |
|     - 你用什麼資料？                                      |
|                                                            |
|  2. 方法 (3 min)                                          |
|     - 資料處理重點                                        |
|     - 模型選擇與比較結果                                  |
|     - 不要念程式碼！用圖表和結果                          |
|                                                            |
|  3. 結果與分析 (3 min)                                    |
|     - 最佳模型的表現                                      |
|     - 關鍵發現（特徵重要性、錯誤分析）                    |
|     - 學到了什麼？                                        |
|                                                            |
|  4. 結論與建議 (2 min)                                    |
|     - 商業價值                                            |
|     - 模型限制                                            |
|     - 未來改進方向                                        |
+----------------------------------------------------------+
```

### ⚠️ 常見陷阱

```
陷阱 1：簡報上放太多程式碼
─────────────────────────────────────────────
聽眾看不懂密密麻麻的程式碼。
用圖表、表格、重點結果取代。

陷阱 2：只講技術，不講故事
─────────────────────────────────────────────
最好的 ML 簡報是在講一個故事：
"有一個問題 → 我們如何解決 → 結果如何 → 接下來怎麼辦"

陷阱 3：忽略模型的限制
─────────────────────────────────────────────
承認限制不是弱點，是專業的表現。
每個模型都有限制，能說出來代表你真的理解。

陷阱 4：沒有準備 demo
─────────────────────────────────────────────
如果可以的話，準備一個簡單的 demo。
看到模型「活著」運作，比看 slides 有說服力 100 倍。

陷阱 5：超時
─────────────────────────────────────────────
練習，練習，再練習。
10 分鐘的簡報至少練 3 遍。
```

### ❓ 沒有笨問題

**Q：我的模型表現不好怎麼辦？**
> A：表現不好不代表專案失敗！重要的是你的分析：
> 為什麼表現不好？資料不足？特徵不夠？問題本身很難？
> 好的分析比好的 accuracy 更有價值。

**Q：可以用 ChatGPT / Copilot 幫忙寫程式嗎？**
> A：可以作為工具使用，但你必須理解每一行程式碼的含義。
> 如果被問到「這行在做什麼？」你答不出來，那就是問題。
> AI 是助手，不是代筆。

**Q：資料集要自己找嗎？**
> A：建議使用公開資料集（Kaggle, UCI ML Repository,
> Google Dataset Search）。選擇你感興趣的領域，
> 這樣做專案會更有動力。

**Q：可以做回歸問題嗎？還是一定要分類？**
> A：都可以！選擇適合你資料和問題的類型。
> 回歸和分類的專案流程其實很類似，
> 差別主要在評估指標的選擇。

---

## 📝 課後練習

### 練習 1：專案提案書
```
寫一份 1 頁的專案提案書，包含：
1. 問題描述（2-3 句）
2. 資料來源（從哪裡取得、大小）
3. 預計使用的方法
4. 成功標準
5. 時間規劃（2 週）
```

### 練習 2：Mini 專案
```
選擇 sklearn 的內建資料集，完成以下流程：
1. EDA（至少 5 張圖）
2. 前處理（使用 Pipeline）
3. 比較 3 個模型（使用 cross_val_score）
4. 學習曲線分析
5. 最終評估報告
6. 限制討論（至少 3 點）

限時：4 小時
```

### 練習 3：互評
```
和同學交換專案，依照評分規準 (Rubric) 互評。
特別注意：
1. 有沒有資料洩漏？
2. Cross validation 用法正確嗎？
3. 模型選擇有道理嗎？
4. 限制討論是否誠實？
```

---

> **恭喜你！** 完成了整門 scikit-learn 課程。
> 記住：ML 不是終點，而是起點。
> 保持好奇心，持續學習，最重要的是 — 動手做專案！
>
> 「紙上得來終覺淺，絕知此事要躬行。」— 陸游
